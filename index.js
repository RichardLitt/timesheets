(function(){
  
  var moment = require('moment');
  var handelbars = requite('handelbars');
  
  var today = moment().format();

  console.log(today);

  // Make a way of templating this thing. 

})();